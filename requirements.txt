# Reflex Cloud build manifest for the AUCTArena app (Python 3.12).
# reflex pulls fastapi/uvicorn/sqlmodel itself; the legacy Streamlit app is not deployed here.
reflex==0.9.*
requests

# Scoring + schedule scraping (season engine, Phase 7-8)
pandas
beautifulsoup4
lxml
scikit-learn
cloudscraper
curl_cffi
tls-client
