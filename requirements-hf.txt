# Lean cloud deps (Render free = 512MB). The WhoScored scrapers + scikit-learn pushed
# startup memory over 512MB (OOM), so they're temporarily out while we make the build
# memory-fit. The core app (login, rooms, bidding, trades, standings, manual scoring)
# runs at ~186MB. pandas stays (Reflex requires it).
reflex==0.9.4
requests
pandas
beautifulsoup4
lxml
