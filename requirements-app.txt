# New Reflex app dependencies (Python 3.12). Install into .venv:
#   python3.12 -m venv .venv && ./.venv/bin/pip install -r requirements-app.txt
# Pinned EXACTLY — see requirements.txt: liveness.py depends on a 0.9.3-internal path.
reflex==0.9.3
requests
pytest

# Scoring + schedule scraping (ported season engine / Phase 7-8)
pandas
beautifulsoup4
lxml
cloudscraper
curl_cffi
tls-client
scikit-learn
